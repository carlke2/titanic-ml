"""
model.py
--------
Model training and prediction functions will go here.

Planned contents:
    - build_baseline_model()  : Create and return a baseline scikit-learn classifier
                                (e.g., LogisticRegression or RandomForestClassifier)
    - train_model(model, X_train, y_train) : Fit the model on training data
    - predict(model, X)       : Return predictions for a given feature matrix
    - cross_validate_model(model, X, y, cv=5) : Run k-fold cross-validation and return scores
    - save_model(model, path) : Persist the trained model to disk (e.g., with joblib)
    - load_model(path)        : Load a previously saved model from disk
"""

# TODO: Implement model functions in a future step.
