"""
helpers.py
----------
General helper and utility functions will go here.

Planned contents:
    - load_data(filepath)     : Load a CSV file and return a DataFrame
    - save_submission(df, filepath) : Save predictions as a Kaggle submission CSV
    - display_info(df)        : Print shape, dtypes, and head of a DataFrame
    - set_seed(seed)          : Set random seeds for reproducibility
"""

# TODO: Implement helper functions in a future step.
