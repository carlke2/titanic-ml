"""
preprocessing.py
----------------
Data cleaning and missing value handling functions will go here.

Planned contents:
    - fill_age(df)           : Impute missing Age values (e.g., with median)
    - fill_embarked(df)      : Fill missing Embarked values with mode
    - fill_fare(df)          : Fill missing Fare values with median
    - drop_unused_cols(df)   : Drop columns not useful for modelling (e.g., Cabin, Ticket, Name)
    - encode_sex(df)         : Encode the Sex column to numeric (male=0, female=1)
    - encode_embarked(df)    : One-hot or label encode the Embarked column
"""

# TODO: Implement preprocessing functions in a future step.
